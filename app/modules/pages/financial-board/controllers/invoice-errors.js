/**
 * @ngdoc function
 * @name app.controller:app.financialBoard.controller.invoiceErrors
 * @description
 * # app.financialBoard.controller.invoiceErrors
 * Controller of the plsApp
 */
angular.module('app').controller('app.financialBoard.controller.invoiceErrors', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Invoice Errors';
  }
]);