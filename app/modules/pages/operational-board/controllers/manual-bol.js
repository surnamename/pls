/**
 * @ngdoc function
 * @name app.controller:app.operationalBoard.controller.manualBOL
 * @description
 * # app.operationalBoard.controller.manualBOL
 * Controller of the plsApp
 */
angular.module('app').controller('app.operationalBoard.controller.manualBOL', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Manual BOL';
  }
]);