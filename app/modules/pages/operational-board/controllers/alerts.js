/**
 * @ngdoc function
 * @name app.controller:app.operationalBoard.controller.alerts
 * @description
 * # app.operationalBoard.controller.alerts
 * Controller of the plsApp
 */
angular.module('app').controller('app.operationalBoard.controller.alerts', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Alerts';
  }
]);