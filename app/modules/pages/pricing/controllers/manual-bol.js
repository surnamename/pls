/**
 * @ngdoc function
 * @name app.controller:app.pricing.controller.manualBol
 * @description
 * # app.pricing.controller.manualBol
 * Controller of the plsApp
 */
angular.module('app').controller('app.pricing.controller.manualBol', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Manual BOL';
  }
]);
