/**
 * @ngdoc function
 * @name app.controller:app.vendorBill.controller.unmatched
 * @description
 * # app.vendorBill.controller.unmatched
 * Controller of the plsApp
 */
angular.module('app').controller('app.vendorBill.controller.saved', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Saved Vendor Bills';
  }
]);