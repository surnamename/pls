/**
 * @ngdoc function
 * @name app.controller:app.reports.controller.unbilled
 * @description
 * # app.reports.controller.unbilled
 * Controller of the plsApp
 */
angular.module('app').controller('app.reports.controller.unbilled', ['$scope',
  function ($scope) {
    'use strict';
    var vm = this;

    $scope.page.title = 'Unbilled';
  }
]);